ignored:
  # DL3029: --platform on ui-builder is intentional — lightningcss (Tailwind v4)
  # has no arm-musl binary; UI output is arch-agnostic static files
  - DL3029
  # DL3018: Alpine package pinning causes build failures when versions rotate
  # out of the repo. We rely on Alpine 3.21 base image pinning instead.
  - DL3018
